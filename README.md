# Himba Coffee

The Next.js site lives in [`himba-coffee/`](./himba-coffee).

```bash
cd himba-coffee
npm install
npm run dev
```

Cloudflare Pages root directory: `himba-coffee`  
Build command: `npm run build`  
Output directory: `out`
